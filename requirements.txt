streamlit==1.54.0
streamlit-webrtc==0.64.5
mediapipe==0.10.14
opencv-python-headless==4.10.0.84
pandas==2.2.3
groq>=0.12.0
gtts==2.5.3
python-dotenv==1.2.2

# AI Voice Trainer - Requirements
# Run: pip install -r requirements.txt

# Core voice libraries
pyttsx3==2.90          # Offline voice (no internet needed)
gTTS==2.5.1            # Online voice (Google TTS - better quality)
pygame==2.5.2          # Audio playback for gTTS

# Pose Detection (if not already installed)
opencv-python==4.9.0.80
mediapipe==0.10.11 
numpy==1.26.4 